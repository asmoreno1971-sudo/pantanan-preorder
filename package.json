{
  "name": "pantanan-preorder",
  "version": "0.1.0",
  "private": true,
  "description": "Pantanan Food Kiosk preorder and kitchen queue app.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/admin.js && node --check public/kitchen.js && node --check public/pos.js && node --check public/qr.js"
  },
  "engines": {
    "node": ">=20"
  }
}
